/**
 * Stub. T-04 replaces this file with the add-task form (`AC-ADD-1..7`).
 * The export name is the contract: `app/(protected)/tasks/page.tsx` imports
 * `{ TaskForm }` from here and is not edited after T-01.
 */
export function TaskForm() {
  return (
    <section aria-labelledby="task-form-heading">
      <h2 id="task-form-heading" className="sr-only">
        Add a task
      </h2>
      <p className="text-muted-foreground text-sm">The add-task form arrives with T-04.</p>
    </section>
  );
}
