# BLOCKERS.md — findings that go back to the Architect

> A Builder or QA session that cannot proceed as specified **stops and writes
> a row here**. It does not decide, and it does not edit a spec file
> ([`CLAUDE.md`](../CLAUDE.md) §Roles). The Architect resolves the row in a
> `docs:` commit and records the resolution beside it. Nothing in this file
> is closed by the session that opened it.

## When to write one

- A task needs to write a file its **File ownership** row does not give it
  ([`TASKS.md`](TASKS.md)).
- A frozen contract from T-01 is wrong or missing a case.
- `TASKS.md` and `ACCEPTANCE.md` disagree, or a criterion cannot be built as
  written.
- A dependency the plan does not list is needed (rule 4 — an ADR comes first).
- QA (T-13) finds a criterion unmet or a test that names an ID and proves
  nothing.

## Format

One row per finding. `B-NN` is sequential across the project.

| Column | Contents |
|---|---|
| ID | `B-NN` |
| Date | when it was raised |
| Raised by | task ID and role — `T-07 Builder`, `T-13 QA`, `ARCH-03 Architect` |
| Finding | what contradicts what, with the file and the line or criterion ID |
| Resolution | what the Architect changed, or `open` |
| Commit | the `docs:` commit that resolved it |

Anyone may append a row. `docs/` is outside every task's ownership lane, so
appending here never fails `/task-close`. The Architect fills in the last two
columns; a Builder never does.

## Log

| ID | Date | Raised by | Finding | Resolution | Commit |
|---|---|---|---|---|---|
| B-01 | 2026-09-01 | ARCH-03 Architect (critic pass) | **No caller could present the key.** ADR-0004 made the Route Handler the key-requiring upstream; ADR-0005 and `AC-API-3` forbid the browser, its only caller, from holding the key. Every request was a `401` by construction. | Two server-side layers: Route Handlers hold the key and call `lib/server/upstream.ts`, which demands it. `AC-API-3..4`, `AM-2`, ADR-0004 (amended section), ADR-0005, T-01 `types/api.ts`, T-06. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-02 | 2026-09-01 | ARCH-03 Architect | **Rule 5 was unsatisfiable.** Rule 5, `AC-TEST-1`, T-11's done-when and DoD #1 required a named Jest test for every criterion; ADR-0006 says `AC-UI-1..4` and `AC-A11Y-4` cannot have one, and `AC-CI-2` and `AC-DEP-1` are a setting and a phone. | New `◉` status, limited by name to those seven criteria, with procedure and date required. `ACCEPTANCE.md` legend, `AC-TEST-1`, `CLAUDE.md` rule 5, `PROJECT.md` DoD #1, T-09, T-10, T-11. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-03 | 2026-09-01 | ARCH-03 Architect | **The wave gate deadlocked on T-14.** `task-start` blocks any wave-5 task until every wave-4 task is closed; T-14 sat at "wave 4–5" and could not close until T-13 (wave 5) had. T-11 would never open. | T-14 moved to wave 5 alongside the T-11 → T-13 chain. Wave 5 is 135m and T-14 is 120m, so wall clock is unchanged. `TASKS.md` Sequence and wave tables, T-14 detail. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-04 | 2026-09-01 | ARCH-03 Architect | **T-10 would fail its own close.** Prose said `components/tasks/**` is shared by T-09 and T-10 in wave 4; the ownership table gave T-10 only `app/(protected)/**`, and `close.py` enforces the table. | `components/tasks/**` (layout classes only) added to T-10's row; merge order stated in the table. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-05 | 2026-09-01 | ARCH-03 Architect | **Squash merge erased the traceability argument.** Rule 3 puts criterion IDs in branch commits; `CONTRIBUTING.md` squash-merges with the PR title as the subject, and nothing required IDs in titles. `main` already showed it (`T-00: wave-aware …`, no prefix, no ID). | PR title carries task ID and criterion IDs: `CLAUDE.md` rule 3, `CONTRIBUTING.md`, `TASKS.md` rule 1, PR template section. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-06 | 2026-09-01 | ARCH-03 Architect | **Criteria assigned to tasks that cannot prove them.** `AC-API-1..2` (a request is sent when I create/delete) on T-06, a server task that renders nothing; the rollback clause of `AC-API-7` and the message clause of `AC-API-12` on T-07, a library task, while both behaviours are T-08's. | `AC-API-1..2` moved to T-08; `AC-API-7` and `AC-API-12` split `(client)` / `(rollback)` and `(message)` the way `AC-API-10` already was. Sequence table, T-06, T-07, T-08. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-07 | 2026-09-01 | ARCH-03 Architect | **Nobody owned the live region.** `AC-FILT-6` (T-05, wave 2), `AC-DEL-2` and `AC-API-11` (T-08, wave 3) and `AC-A11Y-3` (T-09, wave 4) need one announcement mechanism; no path in the ownership table held it. | `components/ui/live-region.tsx` added to T-01's contract table (`components/ui/**` is frozen after wave 0), mounted by T-02, consumed via `useAnnounce()` by T-05, T-08, T-09. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-08 | 2026-09-01 | ARCH-03 Architect | **"Pending" meant two things in the frozen contract.** `SyncState` used `pending` for in-flight; `Filter` uses `pending` for not-completed; `AC-ADD-1` says a new task's "status is Pending". | `SyncState = 'confirmed' \| 'syncing' \| 'failed'`. T-01 `types/task.ts`, ADR-0004, T-08, `AC-ADD-8`, `AC-API-11` wording. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-09 | 2026-09-01 | ARCH-03 Architect | **`AC-API-3` needed a production build no CI step produced.** The bundle test greps `.next/` output; CI ran typecheck, lint and test only, and T-01 owns CI for the life of the project. | CI runs `next build` then `npm run test:bundle`, a separate Jest config that fails rather than skips when `.next/` is absent. `AC-CI-1`, T-01, DoD #2; `close.py` change scheduled in T-16. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-10 | 2026-09-01 | ARCH-03 Architect | **MSW handlers were frozen but T-07 needed them scriptable.** T-07 must drive repeated `429`s with chosen `Retry-After` values without editing `test/msw/handlers.ts`, a T-01 contract. | T-01 exports handler factories (`handlersFor(script)`) alongside the defaults; T-07 and T-08 compose via `server.use(...)`. T-01 contract table. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-11 | 2026-09-01 | ARCH-03 Architect | **Three documents disagreed on approvals.** `CONTRIBUTING.md` required one approving review; `REPO-PROTECTIONS.md` sets it to zero for a solo maintainer; `TASKS.md` then named the review queue as a likely critical path. | `CONTRIBUTING.md` states the solo rule and when it changes; `TASKS.md` failure mode rewritten as the honest risk (rubber-stamping three diffs at once). | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-12 | 2026-09-01 | ARCH-03 Architect | **"Write a blocker" appeared throughout with no file, format or owner.** QA findings were to "go back as tasks", but QA cannot edit `TASKS.md`. | This file. Referenced from `CLAUDE.md`, `TASKS.md` rule 4, T-13. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-13 | 2026-09-01 | ARCH-03 Architect | **T-08's stated risk was moot and its real one unstated.** "Restoring a deleted task to its original index" is automatic under `AC-LIST-3`'s derived ordering. The real reconciliation risk is `AC-API-8`: a server-assigned `id` or `createdAt` would reorder or remount the row. | Contract: client assigns `id` and `createdAt`, server echoes both; persisted envelope omits `sync`. T-01 `types/task.ts`, `types/api.ts`, `schema.ts`; T-08 detail; notes on `AC-API-8..9`. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-14 | 2026-09-01 | ARCH-03 Architect | **Wave 5 is a chain the gate does not see.** `task-start` treats same-wave tasks as concurrent, so T-12 could open before T-11 closed. | `TASKS.md` rule 2 states that `Depends on` holds inside a wave; the script change is scheduled in T-16. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-15 | 2026-09-01 | ARCH-03 Architect | **Stale numbers across documents.** `PROJECT.md` said 16.5 hours, five ADRs and "the first four tasks"; `TASKS.md` said 17.5 hours and 16h45m; the table summed to 18h. `README.md` said the stack was not chosen. DoD #5 required the ledger to reconcile while `LEDGER.md` states `SETUP-01` never will. | All restated from the Sequence table as the single source; README rewritten; DoD #5 names the exception. A lint for this is T-16. | `docs: ARCH-03 spec amendments from the pre-wave critic pass` |
| B-16 | 2026-09-01 | OPS-01 Operator (`OPERATOR.md` §7) | **Branch naming cannot be followed from the web.** `TASKS.md` rule 1 and `CONTRIBUTING.md` require `feat/t-05-list`; Claude Code on the web assigns `claude/<slug>-<suffix>` and its git proxy allows pushes only to that branch. | Both documents accept platform-named branches; attribution is the ledger row and the PR title, not the branch. | `docs: reconcile OPERATOR.md with ARCH-03; accept platform branch names [B-16, B-17]` |
| B-17 | 2026-09-01 | ARCH-03 Architect | **`OPERATOR.md` merged mid-amendment and contradicted it.** It placed T-14 in wave 4, told Builders and QA to touch nothing under `docs/` (which now includes appending to this file), and had QA "list" findings rather than record them. | §3 prompts append to `BLOCKERS.md`; §6 puts T-14 in wave 5; §5 names the build and bundle steps; `CLAUDE.md` states the one permitted `docs/` edit. | `docs: reconcile OPERATOR.md with ARCH-03; accept platform branch names [B-16, B-17]` |
| B-18 | 2026-09-01 | T-16 Builder | **`spec-lint.py` cannot be wired into CI from T-16's lane.** T-16's detail says the lint is "run by `task-start` and by CI on any pull request touching `docs/`", but the File ownership table gives T-16 only `.claude/skills/**` and `scripts/spec-lint.py`; CI is T-01's "for the life of the project". The lint and the `task-start` hook are built and pass on `docs/` as amended by ARCH-03; the workflow is not written. The drop-in is one job: `on: pull_request` with `paths: ["docs/**", "scripts/spec-lint.py"]`, `actions/checkout@v7`, then `python3 scripts/spec-lint.py` — stdlib only, no setup step, `permissions: contents: read`. Either T-01 adds the job to its CI workflow in wave 0, or the Architect adds `.github/workflows/spec-lint.yml` to T-16's Neither: `.github/workflows/repo-guard.yml` is in T-17's ownership row (wave 1, before wave 2 opens) and the `spec-lint` job lands there, as T-17's detail schedules. T-01's CI workflow is untouched. | `docs: ARCH-04 resolve B-18..B-21, amend OPERATOR.md, decide interventions` |
| B-19 | 2026-09-01 | T-01 Builder | **`task-close` check 5 fails T-01 by construction.** `close.py` (rule 4) diffs `dependencies` against `main` and requires a changed file under `docs/adr/` on the branch. At T-01 every runtime dependency is new relative to `main` — `next`, `react`, `react-dom`, `radix-ui`, `lucide-react`, `class-variance-authority`, `clsx`, `tailwind-merge`, `zod` — and each is already covered by ADR-0001, ADR-0003 or ADR-0004, which a Builder may not edit (`CLAUDE.md` §Roles). The check therefore cannot pass for the one task that installs the stack. Every other check passes. Candidate resolutions, the Architect's to choose: accept a dependency whose name appears in an existing ADR (`docs/adr/*.md`); exempt the task whose ownership row is `everything`; or an Architect commit that touches `docs/adr/README.md` on this branch. The first is the durable one and fits T-16's lane (`.claude/skills/**`). | The first: `task-close` check 5 accepts a new runtime dependency whose package name appears in any file under `docs/adr/`. Script change scheduled in T-17 (`.claude/skills/**`); T-01's row stays closed after the fact, as its `notes` say. | `docs: ARCH-04 resolve B-18..B-21, amend OPERATOR.md, decide interventions` |
| B-20 | 2026-09-02 | T-03 Builder | **Nobody mounts `<TasksProvider>`.** T-03 owns `lib/tasks/provider.tsx` and no layout; T-04 and T-05 read "provider hooks", which throw outside the provider. The ownership table gives `app/(protected)/layout.tsx` to T-02 (wave 1), whose detail names only `<LiveRegion />`, and `app/(protected)/**` wrappers to T-10 (wave 4), two waves after the hooks are first consumed. Either T-02's detail should say it mounts `<TasksProvider>` around the protected children (the natural place: the list is per-app, not per-page, and it is a client component so it sits inside the auth guard), or the Architect names another owner. Until then wave 2 components will render outside the provider. | The T-17 session mounts `<TasksProvider>` inside `RequireAuth` in `app/(protected)/layout.tsx` — one commit, `[AC-STATE-1]`, nothing else in it — and the file is in T-17's ownership row for that commit only. T-02's detail now says the layout carries the mount. | `docs: ARCH-04 resolve B-18..B-21, amend OPERATOR.md, decide interventions` |
| B-21 | 2026-09-02 | T-06 Builder | **No criterion names the Route Handler's `400 invalid_request`.** `types/api.ts` (frozen at T-01) defines `RouteHandlerErrorStatus = 400` and the `invalid_request` code, and T-06's detail says request validation is built from `lib/tasks/schema.ts`. It is built and tested (`test/api/handlers.test.ts`, the describe block that cites this row), but `ACCEPTANCE.md` has no `AC-` for it, so those tests claim no criterion and rule 3 has nothing to reference for that behaviour. Either a new `AC-API-` criterion ("the Route Handler rejects a malformed request with 400 and does not call the upstream") or a note in `AC-API-4` that validation precedes the key check. Not blocking T-06. | New criterion `AC-API-13` (400 `invalid_request`, upstream not called, key not presented), assigned to T-06 in the Sequence table; R7 traceability reads `AC-API-1..13` and the count is 79. The T-17 session renames the `B-21` describe block to name `AC-API-13`, one commit, the file in T-17's row for that commit only. | `docs: ARCH-04 resolve B-18..B-21, amend OPERATOR.md, decide interventions` |
| B-22 | 2026-09-02 | T-10 Builder | **`AC-UI-2` cannot be met from T-10's lane.** The criterion covers every interactive control on a touch viewport, but four of them live in files T-10 does not own: the username and password fields and the Log in button in `app/login/login-form.tsx` (T-02's row; 36px tall, the `Input` and `Button` defaults) and the Log out button in `lib/auth/session-bar.tsx` (T-02's row; 78×32, `size="sm"`). `components/ui/**` is frozen after wave 0, so the primitive defaults cannot change either. Every control in `app/(protected)/**` and `components/tasks/**` is ≥ 44px under a coarse-pointer emulation (`scripts/responsive-check.mjs`, `scripts/responsive-check.md`); those four are the remaining failures it prints. The one-line fix each is the class T-10 used elsewhere — `className="pointer-coarse:h-11"` on the two inputs, `className="pointer-coarse:h-11 pointer-coarse:px-6"` on Log in, `className="pointer-coarse:h-11 pointer-coarse:px-4"` on Log out — but the files need an owner: either T-10's row gains `app/login/login-form.tsx` and `lib/auth/session-bar.tsx` for layout classes, or T-11 carries it in the wave-5 sweep. `AC-UI-2` should be marked `◉` for the task-page controls only until then. | T-12 carries it, on the `B-20` pattern: one commit, `[AC-UI-2]`, nothing else in it, the two files in T-12's ownership row for that commit only. T-12 is the session that promotes the build T-13 verifies, so the fix is deployed before T-13 marks `AC-UI-2` `◉` for every control. T-11 never owned the files and is closed. T-12 detail, ownership table. | `docs: ARCH-05 resolve B-22, assign ACCEPTANCE.md marks to the QA pass` |
| B-23 | 2026-09-02 | ARCH-05 Architect | **Repo Guard's ledger-row job treats any title naming a `T-NN` as a Builder PR.** `pr_guard.py` (T-17) exempts `docs:` and `chore:` titles only when they contain no task id; the ARCH-05 PR was titled `… marks to T-13` and was refused for lacking a T-13 ledger row. Architect PRs routinely name the task they amend. And `repo-guard.yml` runs on bare `on: pull_request` (opened, synchronize, reopened), so fixing the title does not re-run the guard; only a push does. | Convention, no script change: an Architect PR title names blockers and `ARCH-NN`, never a `T-NN`; the task goes in the body. Recorded in `OPERATOR.md` §3 Architect prompt. The guard is left as T-17 wrote it — a Builder title that names a task and omits the row is exactly what it exists to catch. Adding `edited` to the workflow's event types is a one-line `.github/workflows/repo-guard.yml` change in T-17's lane, scheduled for T-15's freeze if it is still wanted. | `docs: ARCH-05 resolve B-22, assign ACCEPTANCE.md marks to the QA pass` |
| B-24 | 2026-09-02 | T-13 QA | **`AC-DEP-1` cannot be marked `◉` from any agent session.** The criterion needs the full path walked on a phone-width viewport against the deployed URL. The URL is live and public (production deployment of `main` d3070fa serves `/` and `/login` with 200, no Vercel login prompt; T-12 verified the key server-side and absent from the client chunks), and the path has been walked under Chromium emulation against local production builds (T-12 at phone width; T-13 at 320 via `scripts/responsive-check.mjs`). But no session has a real device and the container's egress refuses a browser session to vercel.app, so T-13 marks `AC-DEP-1` `◐`, the only criterion not `☑` or `◉`. The operator walks it on a phone (log in, add, filter, complete, delete, reload, log out) and the Architect records the device and date beside the criterion in a `docs:` commit; nothing in the code is waiting on it. Two observations from the QA pass, not failures: the filter is a Radix toggle group, so arrow keys move focus and Space or Enter selects, where a native radio group selects on arrow alone; and the date input accepts a three-digit year (a task rendered "Due Feb 2, 115", overdue). Neither contradicts a criterion. | open | — |
| B-25 | 2026-09-02 | T-12 Builder | **iOS WebKit renders the empty due-date field blank and oversized, and no closed task owns the fix.** The operator's phone walk (`B-24`, iOS Chrome on an iPhone 16, which is WebKit) found `<input type="date">` on `/tasks` with no text while empty and an intrinsic width and height that ignore the `Input` primitive's `h-9 w-full`, so the box overflowed its column and looked unpadded — `AC-UI-1` (no clipping at phone width) and `AC-UI-2` in practice, though `scripts/responsive-check.mjs` runs Chromium and cannot see it. The fix is on this branch: `app/globals.css` drops the native appearance on date inputs, keeps the value pseudo's line height, and draws an iOS-only placeholder (`@supports (-webkit-touch-callout: none)`) while `components/tasks/task-form.tsx` sets `data-empty` on the field; a test names `AC-UI-1`. Both files are outside T-12's lane (its ownership row is the two `B-22` auth files), `app/globals.css` is T-01's and `components/tasks/task-form.tsx` is T-04/T-09/T-10's, all closed. `task-close` refuses the ledger annotation until an owner is named — the `B-20`/`B-22` pattern (one commit, `[AC-UI-1, AC-UI-2]`, the two files in T-12's row for that commit only) fits. | Not the `B-20`/`B-22` pattern: that would repeat for every post-QA bug. The plan gains a maintenance phase — wave 7, and the standing task **T-18** whose ownership row is `everything` except the frozen `components/ui/**` — and this fix is its first session. The branch `claude/ios-chrome-date-picker-f8p67s` retags to T-18 (`scripts/task.sh T-18 AC-UI-1,AC-UI-2`), refreshes its ledger row from the transcript so the row reads T-18, reruns `/task-close` unchanged (check 6 passes on `everything`), and merges under the title `fix(tasks): T-18 iOS date field placeholder and sizing [AC-UI-1, AC-UI-2]` with the row's `notes` naming `B-25`. Its `fix(tasks): T-12 …` branch commit stays as it is: the squash-merge subject is the title. T-12's row is unchanged — `app/globals.css` and `task-form.tsx` are not added to it. `TASKS.md` Sequence and wave tables, ownership table, rule 1, T-15 and T-18 detail; `CLAUDE.md` rule 3; `CONTRIBUTING.md`; `OPERATOR.md` §3. | `docs: ARCH-06 open the maintenance phase, resolve B-25` |
| B-26 | 2026-09-02 | ARCH-07 Architect | **`AC-UI-5` mandates unused source files.** The criterion read "buttons, inputs, checkboxes, selects, and dialogs come from shadcn/ui primitives", and `test/quality/component-boundary.test.ts` proved it by asserting that `components/ui/select.tsx` and `dialog.tsx` exist on disk. No screen renders a select or a dialog. A Builder deleting the two unused files fails a criterion; QA would mark the deletion a regression. Three more primitives (`tabs`, `radio-group`, `separator`) are likewise never imported. | Criterion reworded to what the brief asks: every control the interface renders comes from a shadcn primitive and none is hand-rolled, with no file list. Proof moves to a lint rule on JSX element names outside `components/ui/**` (T-19, `⚙`). The five files and `tw-animate-css` are deleted in T-20. ADR-0003 amended. | `docs: ARCH-07 optimisation phase, resolve B-26..B-27`; marks applied by `docs: T-22 ACCEPTANCE.md status marks` |
| B-27 | 2026-09-02 | ARCH-07 Architect | **Rule 5 forces Jest tests that read configuration.** "A criterion is not met until a test names it" has no exception for criteria that describe the toolchain, so `AC-QUAL-1..2`, `AC-CI-1`, `AC-UI-6` and `AC-TEST-2..4` were each proved by a test that spawns ESLint to read the ESLint config, regex-parses `.github/workflows/ci.yml`, or walks the source tree for import directions — 33 tests across `test/quality/**` and `test/msw/handlers.test.ts`, the slowest of them 3.8s, none giving editor feedback. `test-sweep.test.ts` also hard-codes `79` criteria and `seven` manual ones, so any `ACCEPTANCE.md` edit is a test edit. | A fourth status mark `⚙` (enforced by tooling) for exactly eight criteria, named in the `ACCEPTANCE.md` legend and in `CLAUDE.md` rule 5; each is earned by naming the rule, flag, runner option or CI step. T-19 moves the proofs into `eslint.config.mjs` and `jest.config.mjs` and deletes the meta-tests; `AC-TEST-1`'s sweep stays and derives its counts from the document. ADR-0006 amended. T-22 writes the marks. | `docs: ARCH-07 optimisation phase, resolve B-26..B-27`; marks applied by `docs: T-22 ACCEPTANCE.md status marks` |
| B-28 | 2026-09-02 | T-18 Builder | **No criterion names the presentation route.** The operator asked for the deck to be presented from the deployed app, so T-18 adds `app/presentation/` (outside the protected layout) and serves the `.pptx` from `public/presentation/`. `ACCEPTANCE.md` has no `AC-` for a `/presentation` route, so the route's tests (`test/presentation/deck.test.tsx`) carry no ID and the commit references the criteria whose tests cover the code it touches (`AC-NAV-4`: the guard stays in exactly one place under `app/`; `AC-API-3`: the bundle test runs over the new client chunks). `PROJECT.md` Q-2's default (slides stay out of the repo) is reversed by the operator's instruction and should be restated. | open | |
| B-29 | 2026-09-03 | T-21 Builder | **T-21's "Done when" grep scope reaches files outside its ownership and the standing `components/ui/**` freeze; two other files legitimately keep the pattern as content, not comments.** T-21's detail runs `grep -rE '\b(T-[0-9]{2}\|B-[0-9]+\|ARCH-[0-9]+\|AM-[0-9]+\|Wave [0-9])\b' app components lib types`, but its File ownership row is only `lib/**`, `components/**`, `app/**`, `test/**` — `types/**` is T-20's (closed), not T-21's — and `components/ui/**` has stayed frozen since T-01 for every task after it, T-20's ARCH-07 deletions-only exception aside; T-21 carries no such exception. `types/api.ts`, `types/task.ts` (5 refs) and `components/ui/live-region.tsx` (4 refs) still name `T-NN`/`B-NN`/`ARCH-NN` in their headers, so the grep as literally scoped will not return empty even though everything T-21 actually owns is clean (verified separately over `app components lib test`, `components/ui/**` excluded). Separately, `app/presentation/deck.tsx` and `app/presentation/slides.ts` legitimately keep roughly two dozen literal `T-NN`/`B-NN` matches between them: they are the deck's own content — chart labels and speaker narrative describing the AI-driven build process this presentation documents, not attribution comments about who wrote the file — so only the two files' own header comments were cleaned; scrubbing the slide data would misrepresent the case study. Also for the record: the comment-share figure ("under 15%... it was 29%") is not machine-checked anywhere in the repo, so T-21 measured it with an ad hoc script (a line counts as a comment when it is entirely `//`/`/* */`/`*` content) and landed at roughly 14.8% over `app components lib types`, down from an equivalent-method baseline near 29%; a differently-defined heuristic (e.g. counting any line that contains `//` at all) reads closer to 17%, since a fair share of the reduction converted multi-line JSDoc into single-line trailing comments rather than deleting content outright. | open | — |
| B-30 | 2026-09-03 | T-21 Builder | **`lib/tasks/mutations.ts` still imports React, so "React out of `lib/`" is not literally true after T-21's four listed moves.** T-21's rationale (item 6) states the goal as "`lib/` keeps only what runs without React", and its numbered step 1 lists exactly four files to move to `components/`: `lib/auth/{guards,provider,session-bar}.tsx` and `lib/tasks/provider.tsx`. Doing precisely those four (done on this branch) leaves `lib/tasks/mutations.ts` as the one remaining `lib/` file importing `"react"` (`createContext`, `useContext`, `useMemo`, and the exported hook `useTaskMutations`) — confirmed by `grep -rl 'from "react"' lib/` returning only that file. It sits in `lib/` because it is business logic (the apply → call → reconcile/rollback sequences) rather than a component, but the hook binding at its bottom is unmistakably React. Moving it was not authorised by step 1, and the file is outside `components/ui/**`'s freeze, so it is within T-21's `lib/**`/`components/**` ownership either way — the Architect's call is whether to split `useTaskMutations` out to `components/tasks/` (or `lib/tasks/mutations.ts` in full) now, in a T-18 maintenance session, or to leave it as the one accepted exception and correct the rationale sentence instead. | open | — |
| B-31 | 2026-09-03 | T-18 Builder | **A new site nav bar (operator request, no `AC-` of its own) shifts the tab order `AC-A11Y-4`'s dated `◉` record describes.** The operator asked for a responsive nav bar — links to `/presentation` and the `docs/deep-dive` technical walkthrough, plus a `/tasks`-or-`/login` link that follows auth status — shown on `/login` and the protected layout (`app/login/page.tsx`, `app/(protected)/layout.tsx`; new `components/navigation/site-nav.tsx`). `ACCEPTANCE.md` has no criterion for site-wide navigation chrome, so the commit cites the criteria whose tests cover the touched code: `AC-NAV-1`, `AC-NAV-2` (the nav renders on the login and task-list routes) and `AC-UI-1`, `AC-UI-3` (it wraps at 320px and sits in one row at 1024px — reconfirmed with `scripts/responsive-check.mjs`, all 18 checks passing, including the 12→14 count of ≥44px controls on `/tasks`). The nav's three links are now the first three tab stops on `/tasks`, ahead of Log out, which is what `AC-A11Y-4`'s `◉` record (T-13, 2026-09-02, `docs/ACCEPTANCE.md` line 715) walked as stop one; `test/a11y.test.tsx`'s `AC-A11Y-4` keyboard-walk test is updated to the new order and passes, with no trap and focus visible throughout (unchanged CSS), but the record's own dated prose ("Tab to Log out, Enter lands on `/login`" as the first stop) is now inaccurate and its `26 stops` count is stale. No code is blocked on this — the fix is built, tested and merged either way. | open | — |
